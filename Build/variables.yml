variables:
  solution: "BiDiB-Library.Core.sln"
  buildPlatform: "Any CPU"
  buildConfiguration: "Release"
  version: "1.0.4"
  revision: $[counter(variables['version'], 0)]
  packageVersion: '$(version)-dev.$(revision)'
